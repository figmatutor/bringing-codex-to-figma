interface:
  display_name: "Figma Set Text Helper"
  short_description: "Thin helper for batch TEXT updates; use with figma-use"
  default_prompt: "Use $figma-set-text as a helper skill after loading $figma-use; apply mapped batch TEXT replacements and report per-node success/failure."
